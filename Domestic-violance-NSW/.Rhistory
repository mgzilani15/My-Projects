#p
#head(otherdata)
library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- Rate
head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
newdata <- otherdata[ which(Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
newdata <- otherdata[ which(Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
newdata <- otherdata[ which(Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
newdata <- otherdata[ which(Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
newdata <- otherdata[ which(Rank<11),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<11),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
head(otherdata)
#library(ggplot2)
#names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
#names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
#names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
#head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
#newdata <- otherdata[ which(otherdata$Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
head(otherdata)
#library(ggplot2)
#names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
#names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
#names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
#head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
#newdata <- otherdata[ which(otherdata$Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
library(readxl)
otherdata<- read_excel("C:/Users/golam/Desktop/assesment-data/data/otherdata.xls",
sheet="Location")
#df[!is.na(df$B), ]
otherdata[!is.na(otherdata$'Incident Local Government Area'), ]
otherdata[!is.na(otherdata$'Number of incidents'), ]
View(otherdata)
View(otherdata)
head(otherdata)
#library(ggplot2)
#names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
#names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
#names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
#head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
#newdata <- otherdata[ which(otherdata$Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
#names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
#names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
#head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
#newdata <- otherdata[ which(otherdata$Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
#names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
#names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
#head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
#newdata <- otherdata[ which(otherdata$Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
#names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
#head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
#newdata <- otherdata[ which(otherdata$Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
#names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
#head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
#newdata <- otherdata[ which(otherdata$Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
#names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
#head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
#newdata <- otherdata[ which(otherdata$Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
#head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
#newdata <- otherdata[ which(otherdata$Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
#head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
#newdata <- otherdata[ which(otherdata$Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
#newdata <- otherdata[ which(otherdata$Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
#newdata <- otherdata[ which(otherdata$Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
#attach(otherdata)
#newdata <- otherdata[ which(otherdata$Rank<11),]
#head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<11),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<11),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<11),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
library(readxl)
otherdata<- read_excel("C:/Users/golam/Desktop/assesment-data/data/otherdata.xls",
sheet="Location")
#df[!is.na(df$B), ]
otherdata[!is.na(otherdata$'Incident Local Government Area'), ]
otherdata[!is.na(otherdata$'Number of incidents'), ]
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
#otherdata$Rank <- as.numeric(as.character(otherdata$Rank))
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<11),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
#otherdata$Rank <- as.numeric(otherdata$Rank))
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<11),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(otherdata$Rank)
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<11),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(otherdata$Rank)
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<3),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(otherdata$Rank)
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<4),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(otherdata$Rank)
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<5),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(otherdata$Rank)
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<6),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(otherdata$Rank)
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<7),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(otherdata$Rank)
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<8),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(otherdata$Rank)
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<9),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(otherdata$Rank)
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<10),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(otherdata$Rank)
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<11),]
head(newdata)
#p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
#  geom_bar(stat="identity", fill="steelblue")
#p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(otherdata$Rank)
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<11),]
head(newdata)
p<-ggplot(data=otherdata, aes(x=Area,y=Incidents)) +
geom_bar(stat="identity", fill="steelblue")
p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
head(otherdata)
otherdata$Rank <- as.numeric(otherdata$Rank)
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<11),]
head(newdata)
p<-ggplot(data=newdata, aes(x=Area,y=Incidents)) +
geom_bar(stat="identity", fill="steelblue")
p
#head(otherdata)
#library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
#head(otherdata)
otherdata$Rank <- as.numeric(otherdata$Rank)
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<11),]
#head(newdata)
p<-ggplot(data=newdata, aes(x=Area,y=Incidents)) +
geom_bar(stat="identity", fill="steelblue")
p
#head(otherdata)
library(ggplot2)
names(otherdata)[names(otherdata) == "Incident Local Government Area"] <- "Area"
names(otherdata)[names(otherdata) == "Number of incidents"] <- "Incidents"
names(otherdata)[names(otherdata) == "Rate per 100,000"] <- "Rate"
#head(otherdata)
otherdata$Rank <- as.numeric(otherdata$Rank)
attach(otherdata)
newdata <- otherdata[ which(otherdata$Rank<11),]
#head(newdata)
p<-ggplot(data=newdata, aes(x=Area,y=Incidents)) +
geom_bar(stat="identity", fill="steelblue")
p
knitr::opts_chunk$set(echo = TRUE)
# code for upload the initial data files
df_dv_nsw<- read.csv("C:/Users/golam/Desktop/assesment-data/data/DV_NSW_by_LGA.csv")
